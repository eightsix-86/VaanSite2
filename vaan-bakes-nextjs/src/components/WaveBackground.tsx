'use client'

import { useEffect } from 'react'

export default function WaveBackground() {
  useEffect(() => {
    // The animated background is handled by CSS in globals.css
    // This component just ensures the body styling is applied
  }, [])

  return null
}